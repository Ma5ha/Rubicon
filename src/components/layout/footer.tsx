import React from "react";
import Credit from "../credit/credit";

export const Footer = () => {
  const className = "footer";
  return (
    <div {...{ className }}>
      <Credit />
    </div>
  );
};
